# PixelRunner 2.4.0 重构交接

## 1. 项目定位
- 当前项目是 Photoshop UXP 插件重构项目。
- 当前正式开发版本为 `PixelRunnerV2.4.0`。
- 功能对齐基线以 `PixelRunnerV2.3.3` 为准。
- 不再参考 `HHPS v1.3.1`。

## 2. 当前工作目录
- 工作区根目录：
  - `C:\Users\TSUNE\Desktop\PixelRunner重构`
- 当前主项目目录：
  - `C:\Users\TSUNE\Desktop\PixelRunner重构\PixelRunnerV2.4.0`
- 旧版本参考目录：
  - `C:\Users\TSUNE\Desktop\PixelRunner重构\PixelRunnerV2.3.3`

## 3. 当前架构结论
- `2.4.0` 已完成 `UXP Host Shell + WebView UI` 架构切换。
- 必须继续坚持前后端分离和模块化。

### WebView 负责
- UI
- 页面状态
- 表单
- 设置页交互
- 工作台交互
- 任务参数组织

### Host 负责
- bridge
- 存储
- RunningHub 请求
- Photoshop / UXP 能力调用

### 明确禁止
- 不要把复杂 UI 或业务逻辑重新塞回 Host。
- 不要回退到旧的混合式写法。

## 4. 正式源码入口
- Host 构建入口：
  - `src/host-entry.js`
- WebView 构建入口：
  - `src/webview-entry.js`
- 当前正式源码目录：
  - `src/host`
  - `src/webview`

## 5. 页面与构建产物
- UXP Host Shell 页面：
  - `index.html`
- WebView 页面：
  - `app.html`
- 浏览器预览时必须直接打开：
  - `app.html`
- 不要直接在浏览器中打开：
  - `index.html`
- 当前构建产物：
  - `dist/app.bundle.js`
  - `dist/host.bundle.js`

## 6. 当前 Host 结构
- `src/host/main.js`
  - bridge 分发入口
- `src/host/bridge.js`
  - bridge 响应与宿主事件工具
- `src/host/runninghub.js`
  - RunningHub 提交、轮询、取消、账户状态
- `src/host/runninghub-parser.js`
  - RunningHub 应用远程解析、结果归一化、解析调试记录
- `src/host/photoshop.js`
  - Photoshop Host 暴露层
- `src/host/photoshop-bridge.js`
  - Host bridge 到 Photoshop service 的适配
- `src/host/photoshop/service.js`
  - Photoshop 相关核心能力
- `src/host/photoshop/document.js`
  - 文档信息、选区 bounds、文档激活等能力
- `src/host/photoshop/tool-actions.js`
  - 工具箱动作
- `src/host/photoshop/commands.js`
  - Photoshop 命令收敛

## 7. 当前 WebView 结构
- `src/webview/main.js`
  - WebView bootstrap
- `src/webview/runtime.js`
  - bridge client、storage fallback、基础工具
- `src/webview/state.js`
  - settings / apps / templates / form / task / imageCapture 等共享状态
- `src/webview/ui.js`
  - tabs、日志、通用 UI 行为、工具按钮绑定
- `src/webview/apps.js`
  - 应用列表管理、应用编辑器、应用远程解析接入
- `src/webview/templates.js`
  - 提示词模板管理与工作台模板选择器
- `src/webview/settings.js`
  - 设置页读写、账户状态、环境诊断、解析调试读取
- `src/webview/workspace.js`
  - 工作台动态表单、图像输入、任务提交、轮询、取消、自动贴回 Photoshop

## 8. 根目录旧源码状态
- 根目录旧主源码不再作为正式开发入口。
- 旧文件已归档到：
  - `legacy/root-src`
- 后续开发必须继续以 `src/webview` 和 `src/host` 为正式源码。

## 9. 当前已接通的 bridge
- `host.ping`
- `storage.getItem`
- `storage.setItem`
- `runninghub.fetchAccountStatus`
- `runninghub.submitTask`
- `runninghub.pollTask`
- `runninghub.cancelTask`
- `runninghub.parseApp`
- `photoshop.getActiveDocumentInfo`
- `photoshop.captureDocumentPreview`
- `photoshop.placeResultFromUrl`
- `photoshop.runToolAction`

## 10. 当前功能进度

### 10.0 迁移总状态
- `2.4.0` 的前后端拆分迁移已基本完成。
- 当前正式开发、构建、打包、测试链路都已经稳定落在 `src/webview` / `src/host`。
- 当前阶段重点已从“完成迁移”转为“收尾校验、体验收敛、真实插件环境回归验证”。
- 后续不建议再发散重做大架构，优先做小步修正和稳定性验证。

### 10.1 设置页
- 已完成基础设置读写：
  - RunningHub API Key
  - pollInterval
  - timeout
- 已接通 host storage
- 已接通账户状态查询
- 已把高级设置和环境诊断收敛到底部折叠区
- 最近已补齐：
  - 设置页主要乱码文案清理
  - 设置保存和诊断反馈优化
  - 解析完成后的诊断摘要提示

### 10.2 应用管理
- 已完成：
  - 新建应用
  - 编辑应用
  - 删除应用
  - 导入 / 导出 JSON
  - 设置当前应用
  - 同步到 workspace
- 已完成的增强：
  - 列表“当前使用 / 正在编辑”状态标记
  - 编辑器未保存提示
  - 输入结构 JSON 摘要
  - 应用列表搜索 / 排序第一版
  - 同 `appId` 的覆盖式保存
- 已接入：
  - Host 侧远程应用解析链路
  - WebView 不再只做 URL 提取和空壳提示
  - 解析结果会回填应用名、描述、输入结构
  - 保存后可直接进入工作台使用
- 最近已增强：
  - 输入结构摘要更细，包含必填/选填、图像、提示词、选项、布尔项和字段预览
  - 解析成功反馈更明确
  - 浏览器预览模式提示更清楚

### 10.3 解析调试
- Host 在解析应用时会把最近一次调试信息写入：
  - `rh_last_parse_debug`
- 设置页环境诊断区已提供“加载最近解析调试”按钮
- 当前可直接在面板内查看最近一次解析调试 JSON

### 10.4 工作台动态表单与任务流程
- 已完成第一版主流程：
  - 根据 `inputs` 动态渲染表单
  - 表单值采集
  - 必填校验
  - 组装 RunningHub payload
  - 提交任务
  - 轮询结果
  - 取消任务

### 10.5 结果自动贴回 Photoshop
- 当前已改为：
  - 任务完成后自动贴回 Photoshop
  - 不再保留“放回 Photoshop”手动按钮
  - 不再保留手动贴回设置面板
- 当前自动贴回逻辑：
  - 优先以首个图像输入捕获时的源文档信息作为回贴依据
  - 默认允许跨文档回到源文档
  - 如果源文档存在选区，则按原选区 bounds 自动贴回并加遮罩
  - 如果没有选区，则自动回贴到源文档
  - 如果结果已完成但自动贴回失败，任务状态仍保留为成功，并额外提示贴回失败原因
- 说明：
  - 代码迁移与构建已完成
  - 关键边界链路已经做过一轮源码级对齐检查
  - 真实 Photoshop 行为仍需继续在插件环境中验证

### 10.6 图像输入捕获与上传前处理
- 已完成第一版增强：
  - 从 Photoshop 当前文档捕获图像
  - WebView 预览
  - JPEG 质量与最大边长控制
  - 多次捕获资产列表
  - 当前选中捕获图像
  - 将不同捕获图像绑定到不同 image/file 字段
  - 删除单个捕获时自动清理字段绑定
  - 一键清空全部捕获
- 当前备注：
  - 预览图与上传图已分离
  - 上传压缩已补齐长边压制与体积阈值控制
  - `workspace.js` 仍有一部分 UI 文案和体验待继续收敛

### 10.7 工具箱
- 当前已接通并验证基本链路：
  - 黑白观察层
  - 中性灰图层
  - 盖印图层
  - 高斯模糊
  - 锐化
  - 高反差保留
  - 内容识别填充
  - 选择并遮住
- 最近已补齐：
  - 工具箱主链路文案清理
  - 浏览器预览模式提示优化

### 10.8 提示词模板链路
- 当前 `2.4.0` 已接入模板完整第一版：
  - 列表读取
  - 新建
  - 编辑
  - 删除
  - 导入 / 导出 JSON
  - 工作台 prompt-like 字段模板按钮
  - 工作台模板选择弹窗
  - 选中模板后写回字段值
- 最近已继续增强：
  - 工作台模板选择器搜索
  - 模板替换 / 追加写入
  - 模板长度提示
  - 模板模块主要文案乱码清理
  - 模板保存 / 删除 / 选择反馈优化

### 10.9 页面与样式修复
- 之前出现过：
  - `app.html` 标签结构损坏
  - 页面之间串位
  - 页面文案乱码
  - 坏闭合标签直接显示到界面
- 已处理：
  - 重写 `app.html`，恢复三页结构与弹窗结构
  - 修复多处 WebView 模块文案
  - 修复顶部三个栏下方内容挤压标题栏的样式问题
- 当前说明：
  - 结构损坏问题已修复
  - 仍可能有少量旧文案或乱码残留在个别模块里，尤其是 `workspace.js` 和 `app.html`

## 11. 当前未完成或未对齐部分

### 11.0 当前阶段判断
- 当前不是“迁移未完成”，而是“迁移主体已完成，仍有收尾项和回归验证项”。
- 接手时不要再把主要时间花在重搭架构或回退实现方式上。
- 后续工作应优先围绕：
  - 真实 Photoshop 环境边界验证
  - UI 与文案收敛
  - 小范围稳定性修复

### 11.1 设置页与 `2.3.3` 体验仍未完全对齐
- 当前设置页可用，但交互细节仍未完全收敛到 `2.3.3`
- 还可继续加强：
  - 应用解析结果展示方式
  - 应用编辑器保存后的反馈细节
  - 列表操作后的回填、高亮、引导一致性

### 11.2 提示词模板体验仍需继续收敛
- 模板链路虽然已可用，但仍不是最终体验
- 仍需补强：
  - 更接近 `2.3.3` 的模板交互
  - 更稳的 prompt 字段识别
  - 模板组合策略与长度反馈细节
  - 工作台中更清晰的模板应用反馈

### 11.3 UI 文案与残留乱码仍需继续清理
- 严重结构问题已修复
- 但由于此前文件曾被错误编码或错误写回，后续仍要继续检查：
  - `src/webview/workspace.js`
  - `app.html`
  - 其他会直接输出到界面的 WebView 模块
- 原则：
  - 遇到乱码不要只修 `dist`
  - 必须修源码后重新构建

## 12. 当前构建与测试状态

### 构建命令
- `npm.cmd install`
- `npm.cmd run build`
- `npm.cmd run package:test`

### 测试包目录
- `C:\Users\TSUNE\Desktop\PixelRunner重构\PixelRunnerV2.4.0\release\PixelRunnerV2.4.0-test`

### 当前验证状态
- `npm.cmd run build` 可通过
- `npm.cmd run package:test` 可通过
- 当前 `release/PixelRunnerV2.4.0-test` 已按最新源码重新生成，可直接测试

## 13. 下一位接手人的优先顺序

### 第一优先级
- 在真实 Photoshop 插件环境里继续做边界回归：
  - 跨文档自动贴回
  - 源文档关闭后的行为
  - 多图输入时首图蒙版语义
- 继续把 `2.4.0` 设置页和工作台交互向 `2.3.3` 收敛
- 继续清理 WebView 源码中的残留乱码与旧文案

### 第二优先级
- 继续完善应用解析体验，不要只停留在当前已接入链路
- 继续完善提示词模板体验
- 继续完善应用列表管理：
  - 搜索
  - 排序
  - 保存 / 删除 / 编辑反馈
- 继续提升图像输入链路：
  - 更清晰的字段级映射
  - 多图字段 / 数组字段支持
  - 更明确的上传策略可视化

### 第三优先级
- 继续工程化和收敛：
  - 保持 `src/webview` / `src/host` 为唯一正式源码
  - 继续减少迁移过程中的结构噪音
  - 不要重新引入旧版混合式写法

## 14. 一句话结论
- 当前 `2.4.0` 已完成架构切换，而且可以继续迭代；接下来最重要的不是发散新逻辑，而是严格围绕 `2.3.3` 做功能与交互收敛，同时守住现有前后端分离结构。
## 最新同步（2026-04-07）

### 当前项目状态摘要
- `PixelRunnerV2.4.0` 的 `UXP Host Shell + WebView UI` 架构继续有效，没有回退。
- 当前主开发区域仍然是 `src/webview` 和 `src/host`。
- 当前主构建入口仍然是 `src/webview-entry.js` 和 `src/host-entry.js`。
- `release/PixelRunnerV2.4.0-test` 仍是可直接测试的打包目录。
- 当前可以判断：迁移主体已经基本完成，项目已进入收尾与验证阶段。

### 最近已经落地的重点
- 工作台界面继续向 `PixelRunnerV2.3.3` 靠拢，不再保留过多摘要型占位内容。
- “选择应用”区域已往紧凑布局调整，目标是减少上下高度占用。
- 当前应用展示已尽量简化为只看应用名，不再默认展开应用 ID、输入项数量、图像项数量等信息。
- 输入参数区正在持续做减法，只保留用户真正会操作的图像输入和参数输入。
- 应用切换弹窗与模板选择弹窗已加长，能容纳更多卡片。
- 设置页修改最大并发任务数后，工作台并发显示已同步刷新。
- 插件名称已更新为 `PixelRunner（小T修图助手）`。

### 图像输入链路的最新实现
- Host 侧 `photoshop.captureDocumentPreview` 已补上优先读取 Photoshop 当前选区的逻辑。
- 若当前存在选区，则优先按选区范围捕获图像。
- 若没有选区，则自动回退到当前文档整张图像。
- WebView 侧图像字段已朝 `2.3.3` 的交互方式迁移：让字段本身成为点击捕获入口。
- 上传链路已补齐长边压制与体积阈值控制，降低大图上传失败风险。
- 后续继续迭代时，不要再把“绑定关系”“上传方式”“dataUrl”等工程细节展示给用户。

### 工作台任务区的最新实现
- WebView 状态侧已从只关注单个 `runningTask`，扩展为开始维护 `runningTasks` 列表。
- 当前方向是支持多个进行中任务并允许逐个取消，不要在后续改动里退回单任务限制。
- 任务区视觉和布局还没有完全收敛到 `2.3.3`，这是下一阶段重点之一。
- 自动贴回链路已补过一轮边界修正：
  - 优先以首图/首个图像输入的源文档语义作为回贴依据
  - 自动贴回失败不再把整任务误判为失败

### 仍需继续处理的重点
- 在真实 Photoshop 环境中继续验证跨文档回贴、源文档关闭、结果贴回安全提示等边界。
- 继续把工作台布局、密度、交互节奏向 `2.3.3` 收敛，尤其是运行任务 / 取消任务区域。
- 继续简化图像输入区域，只保留直观捕获、预览、清空这些核心动作。
- 继续完善应用解析体验，不要只停在当前第一版诊断反馈。
- 继续完善提示词模板体验。
- 继续清理 `workspace.js`、`templates.js`、`settings.js` 里的残留乱码、旧文案和不一致提示。

### 接手时不要偏离的原则
- 不要回到根目录旧源码做主开发。
- 不要直接改 `dist/*.bundle.js`。
- 不要把复杂 UI 或业务逻辑重新塞回 Host。
- WebView 负责 UI、状态、表单、交互。
- Host 负责 bridge、存储、RunningHub、Photoshop 能力。

### 当前建议的下一步优先顺序
1. 在真实 Photoshop 环境里回归验证跨文档自动贴回、源文档关闭、首图蒙版依据这几条关键边界。
2. 继续把 `src/webview/workspace.js` 的工作台布局和图像输入体验磨到更接近 `2.3.3`。
3. 继续重做运行任务 / 取消任务区域，兼顾简洁界面和“可取消任意进行中任务”。
4. 继续清理 `src/webview/templates.js` 和 `src/webview/settings.js` 的残留乱码与提示文案。
5. 每次改完源码后执行 `npm.cmd run build` 和 `npm.cmd run package:test`。
